# owner-signal-sema-upgrade

`owner-signal-sema-upgrade` is the owner-only policy signal contract for
the `sema-upgrade` component.

The prototype surface registers which migration modules an owner accepts
and records allow/block policy for ordinary upgrade attempts.
